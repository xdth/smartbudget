{% extends 'base.html.twig' %}

{% block title %}Edit Budget{% endblock %}

{% block body %}
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a class="link-dark" href="{{ path('index') }}">Home</a></li>
        <li class="breadcrumb-item"><a class="link-dark" href="{{ path('budget_index') }}">Budget</a></li>
        <li class="breadcrumb-item active" aria-current="page">{{ budget.id }}</li>
      </ol>
    </nav>

    <div class="row">
      <div class="col-sm-8">
        <h1>Edit Budget</h1>
      </div>
      <div class="col-sm-4">
        <div class="app-inline-buttons">
          {{ include('budget/_delete_form.html.twig') }}
          <a class="btn btn-dark" href="{{ path('budget_index') }}">Back</a>
        </div>
      </div>
    </div>

    {{ include('budget/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}
